'use strict';

/**
 * kommende-ausstellungen router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::kommende-ausstellungen.kommende-ausstellungen');
