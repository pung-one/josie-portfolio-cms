'use strict';

/**
 * kommende-ausstellungen service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::kommende-ausstellungen.kommende-ausstellungen');
